DEBUG=True
SECRET_KEY=change-me-in-production
DATABASE_URL=postgresql://nexus:nexus@localhost:5432/nexus

# Embeddings API — remote service, no local model needed.
# In-cluster default: http://embeddings.embeddings.svc.cluster.local:8000/embed
# Local dev (DEBUG=True) falls back to: https://embeddings.lab.amazingland.live/embed
# Override with:
# EMBEDDINGS_API_URL=https://embeddings.lab.amazingland.live/embed

ALLOWED_HOSTS=localhost,127.0.0.1,nexus.lab.amazingland.live
