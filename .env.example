DEBUG=True
SECRET_KEY=change-me-in-production
DATABASE_URL=postgresql://nexus:nexus@localhost:5432/nexus

# Embeddings use sentence-transformers (local, no API key required).
# Model: all-MiniLM-L6-v2 — downloaded on first use, or baked into Docker image at build.
# HF_HOME controls where the model cache lives (default: ~/.cache/huggingface)
# HF_HOME=

ALLOWED_HOSTS=localhost,127.0.0.1,nexus.lab.amazingland.live
